import SigninCover from 'views/SigninCover';
import Minimal from 'layouts/Minimal';

export default () => {
  return (
    <div className="container">
      <Minimal>
        <SigninCover />
      </Minimal>
    </div>
  )
}
